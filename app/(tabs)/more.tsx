import React from 'react';
import { View } from 'react-native';

// This is a placeholder component required by Expo Router
// The actual functionality is handled by the custom tabBarButton in _layout.tsx
export default function MoreScreen() {
  return <View />;
}